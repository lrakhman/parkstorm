language: ruby
rvm:
  - 2.0.0
before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
  - cp config/database.yml.travis config/database.yml
script:
  - rake db:test:prepare
  - rake db:test_after_migrate --trace
  - bundle exec rake